[package]
name        = "archet-plugin"
description = "Archet. VST3 / CLAP."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name       = "archet_plugin"
crate-type = ["cdylib", "rlib"]

[dependencies]
nice-plug      = { workspace = true }
libc           = "0.2"
nice-plug-egui = { workspace = true }
archet          = { workspace = true }
archet-ui       = { workspace = true }
egui           = { workspace = true }
serde_json     = { workspace = true }
phonix-plugin  = { workspace = true }   # the `.vstpreset` writer + the `Preset` trait
phonix-rt      = { workspace = true }   # the lock-free meter channel
